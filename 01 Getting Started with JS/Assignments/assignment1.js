var firstName = "Supriya";
var lastName = "Sinha";
var email = "supriya.sinha2k@gmail.com";
var password = "123456";
var country = "India";
var state = "Bihar";
var courseCount = 20;
var isLoggedInFromGoogle = true;
var isLoggedInFromFacebook = false;

console.log(`
    With Full Name: ${firstName} ${lastName}
    and E-mail ID : ${email}
    and uses the password : ${password}
    lives in : ${country}
    and is from the state : ${state}
    with courseCount as : ${courseCount}
    has logged from Google or Facebook; if google,then : ${isLoggedInFromGoogle}
    or from Facebook, then : ${isLoggedInFromFacebook}.
`);